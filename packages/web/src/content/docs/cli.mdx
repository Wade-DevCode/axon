---
title: CLI
description: Axon CLI options and commands.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

The Axon CLI by default starts the [TUI](/docs/tui) when run without any arguments.

```bash
axon
```

But it also accepts commands as documented on this page. This allows you to interact with Axon programmatically.

```bash
axon run "Explain how closures work in JavaScript"
```

---

### tui

Start the Axon terminal user interface.

```bash
axon [project]
```

#### Flags

| Flag                                        | Short | Description                                                             |
| ------------------------------------------- | ----- | ----------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr>    | `-c`  | Continue the last session                                               |
| <nobr><code>{"--session"}</code></nobr>     | `-s`  | Session ID to continue                                                  |
| <nobr><code>{"--fork"}</code></nobr>        |       | Fork the session when continuing (use with `--continue` or `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>      |       | Prompt to use                                                           |
| <nobr><code>{"--model"}</code></nobr>       | `-m`  | Model to use in the form of provider/model                              |
| <nobr><code>{"--agent"}</code></nobr>       |       | Agent to use                                                            |
| <nobr><code>{"--port"}</code></nobr>        |       | Port to listen on                                                       |
| <nobr><code>{"--hostname"}</code></nobr>    |       | Hostname to listen on                                                   |
| <nobr><code>{"--mdns"}</code></nobr>        |       | Enable mDNS discovery                                                   |
| <nobr><code>{"--mdns-domain"}</code></nobr> |       | Custom mDNS domain name                                                 |
| <nobr><code>{"--cors"}</code></nobr>        |       | Additional browser origin(s) to allow CORS                              |

---

## Commands

The Axon CLI also has the following commands.

---

### agent

Manage agents for Axon.

```bash
axon agent [command]
```

---

#### create

Create a new agent with custom configuration.

```bash
axon agent create
```

This command will guide you through creating a new agent with a custom system prompt and permission configuration. Anything you don't allow is denied in the generated agent's frontmatter.

#### Flags

| Flag                                        | Short | Description                                                                                                                                                                                                                |
| ------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--path"}</code></nobr>        |       | Directory to write the agent file to (defaults to global or `.axon/agent` based on the prompt)                                                                                                                         |
| <nobr><code>{"--description"}</code></nobr> |       | What the agent should do                                                                                                                                                                                                   |
| <nobr><code>{"--mode"}</code></nobr>        |       | Agent mode: `all`, `primary`, or `subagent`                                                                                                                                                                                |
| <nobr><code>{"--permissions"}</code></nobr> |       | Comma-separated list of permissions to allow (default: all). Available: `bash`, `read`, `edit`, `glob`, `grep`, `webfetch`, `task`, `todowrite`, `websearch`, `lsp`, `skill`. Anything omitted is denied. Alias: `--tools` |
| <nobr><code>{"--model"}</code></nobr>       | `-m`  | Model to use, in `provider/model` format                                                                                                                                                                                   |

Passing all of `--path`, `--description`, `--mode`, and `--permissions` runs the command non-interactively.

---

#### list

List all available agents.

```bash
axon agent list
```

---

### attach

Attach a terminal to an already running Axon backend server started via `serve` or `web` commands.

```bash
axon attach [url]
```

This allows using the TUI with a remote Axon backend. For example:

```bash
# Start the backend server for web/mobile access
axon web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
axon attach http://10.20.30.40:4096
```

#### Flags

| Flag                                     | Short | Description                                                                |
| ---------------------------------------- | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |       | Working directory to start TUI in                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continue the last session                                                  |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | Session ID to continue                                                     |
| <nobr><code>{"--fork"}</code></nobr>     |       | Fork the session when continuing (use with `--continue` or `--session`)    |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Basic auth password (defaults to `AXON_SERVER_PASSWORD`)               |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Basic auth username (defaults to `AXON_SERVER_USERNAME` or `axon`) |

---

### auth

Command to manage credentials and login for providers.

```bash
axon auth [command]
```

---

#### login

Axon is powered by the provider list at [Models.dev](https://models.dev), so you can use `axon auth login` to configure API keys for any provider you'd like to use. This is stored in `~/.local/share/axon/auth.json`.

```bash
axon auth login
```

When Axon starts up it loads the providers from the credentials file. And if there are any keys defined in your environments or a `.env` file in your project.

##### Flags

| Flag                                     | Short | Description                                          |
| ---------------------------------------- | ----- | ---------------------------------------------------- |
| <nobr><code>{"--provider"}</code></nobr> | `-p`  | Provider ID or name to log in to                     |
| <nobr><code>{"--method"}</code></nobr>   | `-m`  | Login method label to use, skipping method selection |

---

#### list

Lists all the authenticated providers as stored in the credentials file.

```bash
axon auth list
```

Or the short version.

```bash
axon auth ls
```

---

#### logout

Logs you out of a provider by clearing it from the credentials file.

```bash
axon auth logout
```

---

### github

Manage the GitHub agent for repository automation.

```bash
axon github [command]
```

---

#### install

Install the GitHub agent in your repository.

```bash
axon github install
```

This sets up the necessary GitHub Actions workflow and guides you through the configuration process. [Learn more](/docs/github).

---

#### run

Run the GitHub agent. This is typically used in GitHub Actions.

```bash
axon github run
```

##### Flags

| Flag                                  | Description                            |
| ------------------------------------- | -------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | GitHub mock event to run the agent for |
| <nobr><code>{"--token"}</code></nobr> | GitHub personal access token           |

---

### mcp

Manage Model Context Protocol servers.

```bash
axon mcp [command]
```

---

#### add

Add an MCP server to your configuration.

```bash
axon mcp add
```

This command will guide you through adding either a local or remote MCP server.

---

#### list

List all configured MCP servers and their connection status.

```bash
axon mcp list
```

Or use the short version.

```bash
axon mcp ls
```

---

#### auth

Authenticate with an OAuth-enabled MCP server.

```bash
axon mcp auth [name]
```

If you don't provide a server name, you'll be prompted to select from available OAuth-capable servers.

You can also list OAuth-capable servers and their authentication status.

```bash
axon mcp auth list
```

Or use the short version.

```bash
axon mcp auth ls
```

---

#### logout

Remove OAuth credentials for an MCP server.

```bash
axon mcp logout [name]
```

---

#### debug

Debug OAuth connection issues for an MCP server.

```bash
axon mcp debug <name>
```

---

### models

List all available models from configured providers.

```bash
axon models [provider]
```

This command displays all models available across your configured providers in the format `provider/model`.

This is useful for figuring out the exact model name to use in [your config](/docs/config/).

You can optionally pass a provider ID to filter models by that provider.

```bash
axon models anthropic
```

#### Flags

| Flag                                    | Description                                                  |
| --------------------------------------- | ------------------------------------------------------------ |
| <nobr><code>{"--refresh"}</code></nobr> | Refresh the models cache from models.dev                     |
| <nobr><code>{"--verbose"}</code></nobr> | Use more verbose model output (includes metadata like costs) |

Use the `--refresh` flag to update the cached model list. This is useful when new models have been added to a provider and you want to see them in Axon.

```bash
axon models --refresh
```

---

### run

Run axon in non-interactive mode by passing a prompt directly.

```bash
axon run [message..]
```

This is useful for scripting, automation, or when you want a quick answer without launching the full TUI. For example.

```bash "axon run"
axon run Explain the use of context in Go
```

You can also attach to a running `axon serve` instance to avoid MCP server cold boot times on every run:

```bash
# Start a headless server in one terminal
axon serve

# In another terminal, run commands that attach to it
axon run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flags

| Flag                                                         | Short | Description                                                                |
| ------------------------------------------------------------ | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>                      |       | The command to run, use message for args                                   |
| <nobr><code>{"--continue"}</code></nobr>                     | `-c`  | Continue the last session                                                  |
| <nobr><code>{"--session"}</code></nobr>                      | `-s`  | Session ID to continue                                                     |
| <nobr><code>{"--fork"}</code></nobr>                         |       | Fork the session when continuing (use with `--continue` or `--session`)    |
| <nobr><code>{"--share"}</code></nobr>                        |       | Share the session                                                          |
| <nobr><code>{"--model"}</code></nobr>                        | `-m`  | Model to use in the form of provider/model                                 |
| <nobr><code>{"--agent"}</code></nobr>                        |       | Agent to use                                                               |
| <nobr><code>{"--file"}</code></nobr>                         | `-f`  | File(s) to attach to message                                               |
| <nobr><code>{"--format"}</code></nobr>                       |       | Format: default (formatted) or json (raw JSON events)                      |
| <nobr><code>{"--title"}</code></nobr>                        |       | Title for the session (uses truncated prompt if no value provided)         |
| <nobr><code>{"--attach"}</code></nobr>                       |       | Attach to a running axon server (e.g., http://localhost:4096)          |
| <nobr><code>{"--password"}</code></nobr>                     | `-p`  | Basic auth password (defaults to `AXON_SERVER_PASSWORD`)               |
| <nobr><code>{"--username"}</code></nobr>                     | `-u`  | Basic auth username (defaults to `AXON_SERVER_USERNAME` or `axon`) |
| <nobr><code>{"--dir"}</code></nobr>                          |       | Directory to run in, or path on the remote server when attaching           |
| <nobr><code>{"--port"}</code></nobr>                         |       | Port for the local server (defaults to random port)                        |
| <nobr><code>{"--variant"}</code></nobr>                      |       | Model variant (provider-specific reasoning effort)                         |
| <nobr><code>{"--thinking"}</code></nobr>                     |       | Show thinking blocks                                                       |
| <nobr><code>{"--dangerously-skip-permissions"}</code></nobr> |       | Auto-approve permissions that are not explicitly denied (dangerous!)       |

---

### serve

Start a headless Axon server for API access. Check out the [server docs](/docs/server) for the full HTTP interface.

```bash
axon serve
```

This starts an HTTP server that provides API access to axon functionality without the TUI interface. Set `AXON_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `axon`).

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### session

Manage Axon sessions.

```bash
axon session [command]
```

---

#### list

List all Axon sessions.

```bash
axon session list
```

##### Flags

| Flag                                      | Short | Description                          |
| ----------------------------------------- | ----- | ------------------------------------ |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`  | Limit to N most recent sessions      |
| <nobr><code>{"--format"}</code></nobr>    |       | Output format: table or json (table) |

---

#### delete

Delete an Axon session.

```bash
axon session delete <sessionID>
```

---

### stats

Show token usage and cost statistics for your Axon sessions.

```bash
axon stats
```

#### Flags

| Flag                                    | Description                                                                 |
| --------------------------------------- | --------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Show stats for the last N days (all time)                                   |
| <nobr><code>{"--tools"}</code></nobr>   | Number of tools to show (all)                                               |
| <nobr><code>{"--models"}</code></nobr>  | Show model usage breakdown (hidden by default). Pass a number to show top N |
| <nobr><code>{"--project"}</code></nobr> | Filter by project (all projects, empty string: current project)             |

---

### export

Export session data as JSON.

```bash
axon export [sessionID]
```

If you don't provide a session ID, you'll be prompted to select from available sessions.

#### Flags

| Flag                                     | Description                           |
| ---------------------------------------- | ------------------------------------- |
| <nobr><code>{"--sanitize"}</code></nobr> | Redact sensitive transcript/file data |

---

### import

Import session data from a JSON file or Axon share URL.

```bash
axon import <file>
```

You can import from a local file or an Axon share URL.

```bash
axon import session.json
axon import https://opncd.ai/s/abc123
```

---

### web

Start a headless Axon server with a web interface.

```bash
axon web
```

This starts an HTTP server and opens a web browser to access Axon through a web interface. Set `AXON_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `axon`).

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### acp

Start an ACP (Agent Client Protocol) server.

```bash
axon acp
```

This command starts an ACP server that communicates via stdin/stdout using nd-JSON.

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--cwd"}</code></nobr>         | Working directory                          |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### plugin

Install a plugin and update your config.

```bash
axon plugin <module>
```

Or use the alias.

```bash
axon plug <module>
```

#### Flags

| Flag                                   | Short | Description                     |
| -------------------------------------- | ----- | ------------------------------- |
| <nobr><code>{"--global"}</code></nobr> | `-g`  | Install in global config        |
| <nobr><code>{"--force"}</code></nobr>  | `-f`  | Replace existing plugin version |

---

### pr

Fetch and checkout a GitHub PR branch, then run Axon.

```bash
axon pr <number>
```

---

### db

Database tools.

```bash
axon db [query]
```

#### Flags

| Flag                                   | Description                    |
| -------------------------------------- | ------------------------------ |
| <nobr><code>{"--format"}</code></nobr> | Output format: `json` or `tsv` |

---

#### path

Print the database path.

```bash
axon db path
```

---

### debug

Debugging and troubleshooting tools.

```bash
axon debug [command]
```

---

### uninstall

Uninstall Axon and remove all related files.

```bash
axon uninstall
```

#### Flags

| Flag                                        | Short | Description                                 |
| ------------------------------------------- | ----- | ------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`  | Keep configuration files                    |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`  | Keep session data and snapshots             |
| <nobr><code>{"--dry-run"}</code></nobr>     |       | Show what would be removed without removing |
| <nobr><code>{"--force"}</code></nobr>       | `-f`  | Skip confirmation prompts                   |

---

### upgrade

Updates axon to the latest version or a specific version.

```bash
axon upgrade [target]
```

To upgrade to the latest version.

```bash
axon upgrade
```

To upgrade to a specific version.

```bash
axon upgrade v0.1.48
```

#### Flags

| Flag                                   | Short | Description                                                       |
| -------------------------------------- | ----- | ----------------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`  | The installation method that was used; curl, npm, pnpm, bun, brew |

---

## Global Flags

The axon CLI takes the following global flags.

| Flag                                       | Short | Description                          |
| ------------------------------------------ | ----- | ------------------------------------ |
| <nobr><code>{"--help"}</code></nobr>       | `-h`  | Display help                         |
| <nobr><code>{"--version"}</code></nobr>    | `-v`  | Print version number                 |
| <nobr><code>{"--print-logs"}</code></nobr> |       | Print logs to stderr                 |
| <nobr><code>{"--log-level"}</code></nobr>  |       | Log level (DEBUG, INFO, WARN, ERROR) |
| <nobr><code>{"--pure"}</code></nobr>       |       | Run without external plugins         |

---

## Environment variables

Axon can be configured using environment variables.

| Variable                              | Type    | Description                                       |
| ------------------------------------- | ------- | ------------------------------------------------- |
| `AXON_AUTO_SHARE`                 | boolean | Automatically share sessions                      |
| `AXON_GIT_BASH_PATH`              | string  | Path to Git Bash executable on Windows            |
| `AXON_CONFIG`                     | string  | Path to config file                               |
| `AXON_TUI_CONFIG`                 | string  | Path to TUI config file                           |
| `AXON_CONFIG_DIR`                 | string  | Path to config directory                          |
| `AXON_CONFIG_CONTENT`             | string  | Inline json config content                        |
| `AXON_DISABLE_AUTOUPDATE`         | boolean | Disable automatic update checks                   |
| `AXON_DISABLE_PRUNE`              | boolean | Disable pruning of old data                       |
| `AXON_DISABLE_TERMINAL_TITLE`     | boolean | Disable automatic terminal title updates          |
| `AXON_PERMISSION`                 | string  | Inlined json permissions config                   |
| `AXON_DISABLE_DEFAULT_PLUGINS`    | boolean | Disable default plugins                           |
| `AXON_DISABLE_LSP_DOWNLOAD`       | boolean | Disable automatic LSP server downloads            |
| `AXON_ENABLE_EXPERIMENTAL_MODELS` | boolean | Enable experimental models                        |
| `AXON_DISABLE_AUTOCOMPACT`        | boolean | Disable automatic context compaction              |
| `AXON_DISABLE_CLAUDE_CODE`        | boolean | Disable reading from `.claude` (prompt + skills)  |
| `AXON_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disable reading `~/.claude/CLAUDE.md`             |
| `AXON_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disable loading `.claude/skills`                  |
| `AXON_DISABLE_MODELS_FETCH`       | boolean | Disable fetching models from remote sources       |
| `AXON_DISABLE_MOUSE`              | boolean | Disable mouse capture in the TUI                  |
| `AXON_FAKE_VCS`                   | string  | Fake VCS provider for testing purposes            |
| `AXON_CLIENT`                     | string  | Client identifier (defaults to `cli`)             |
| `AXON_ENABLE_EXA`                 | boolean | Enable Exa web search tools                       |
| `AXON_SERVER_PASSWORD`            | string  | Enable basic auth for `serve`/`web`               |
| `AXON_SERVER_USERNAME`            | string  | Override basic auth username (default `axon`) |
| `AXON_MODELS_URL`                 | string  | Custom URL for fetching models configuration      |

---

### Experimental

These environment variables enable experimental features that may change or be removed.

| Variable                                        | Type    | Description                             |
| ----------------------------------------------- | ------- | --------------------------------------- |
| `AXON_EXPERIMENTAL`                         | boolean | Enable the experimental umbrella flag   |
| `AXON_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Enable icon discovery                   |
| `AXON_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disable copy on select in TUI           |
| `AXON_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Default timeout for bash commands in ms |
| `AXON_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Max output tokens for LLM responses     |
| `AXON_EXPERIMENTAL_FILEWATCHER`             | boolean | Enable file watcher for entire dir      |
| `AXON_EXPERIMENTAL_OXFMT`                   | boolean | Enable oxfmt formatter                  |
| `AXON_EXPERIMENTAL_LSP_TOOL`                | boolean | Enable experimental LSP tool            |
| `AXON_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disable file watcher                    |
| `AXON_EXPERIMENTAL_EXA`                     | boolean | Enable experimental Exa features        |
| `AXON_EXPERIMENTAL_LSP_TY`                  | boolean | Enable TY LSP for python files          |
| `AXON_EXPERIMENTAL_PLAN_MODE`               | boolean | Enable plan mode                        |
| `AXON_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Enable background subagent tasks        |
| `AXON_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Enable experimental event system        |
| `AXON_EXPERIMENTAL_NATIVE_LLM`              | boolean | Enable native LLM request path          |
| `AXON_EXPERIMENTAL_PARALLEL`                | boolean | Enable parallel web search execution    |
| `AXON_EXPERIMENTAL_SCOUT`                   | boolean | Enable Scout subagent                   |
| `AXON_EXPERIMENTAL_WORKSPACES`              | boolean | Enable workspace support                |
