---
title: CLI
description: Opzioni e comandi della CLI di Axon.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

La CLI di Axon, per impostazione predefinita, avvia la [TUI](/docs/tui) quando viene eseguita senza argomenti.

```bash
axon
```

Ma accetta anche i comandi documentati in questa pagina. Questo ti permette di interagire con Axon in modo programmatico.

```bash
axon run "Explain how closures work in JavaScript"
```

---

### tui

Avvia l'interfaccia testuale di Axon (interfaccia utente da terminale).

```bash
axon [project]
```

#### Flag

| Flag                                     | Breve | Descrizione                                                              |
| ---------------------------------------- | ----- | ------------------------------------------------------------------------ |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continua l'ultima sessione                                               |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID sessione da continuare                                                |
| <nobr><code>{"--fork"}</code></nobr>     |       | Duplica la sessione quando continui (usa con `--continue` o `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |       | Prompt da usare                                                          |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Modello nel formato provider/model                                       |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agente da usare                                                          |
| <nobr><code>{"--port"}</code></nobr>     |       | Porta su cui mettersi in ascolto                                         |
| <nobr><code>{"--hostname"}</code></nobr> |       | Hostname su cui mettersi in ascolto                                      |

---

## Comandi

La CLI di Axon include anche i seguenti comandi.

---

### agent

Gestisci gli agenti per Axon.

```bash
axon agent [command]
```

---

### attach

Collega un terminale a un backend server di Axon già in esecuzione avviato tramite i comandi `serve` o `web`.

```bash
axon attach [url]
```

Questo consente di usare la TUI con un backend Axon remoto. Per esempio:

```bash
# Start the backend server for web/mobile access
axon web --port 4096 --hostname 0.0.0.0

# In un altro terminale, collega la TUI al backend in esecuzione
axon attach http://10.20.30.40:4096
```

#### Flag

| Flag                                     | Breve | Descrizione                                                                                     |
| ---------------------------------------- | ----- | ----------------------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |       | Working directory in cui avviare la TUI                                                         |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continua l'ultima sessione                                                                      |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID sessione da continuare                                                                       |
| <nobr><code>{"--fork"}</code></nobr>     |       | Crea un fork della sessione durante la continuazione (usa con `--continue` o `--session`)       |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Password per l'autenticazione di base (predefinita: `AXON_SERVER_PASSWORD`)                 |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Nome utente per l'autenticazione di base (predefinito: `AXON_SERVER_USERNAME` o `axon`) |

---

#### create

Crea un nuovo agente con configurazione personalizzata.

```bash
axon agent create
```

Questo comando ti guida nella creazione di un nuovo agente con un system prompt personalizzato e configurazione degli strumenti.

---

#### list

Elenca tutti gli agenti disponibili.

```bash
axon agent list
```

---

### auth

Comando per gestire le credenziali e il login dei provider.

```bash
axon auth [command]
```

---

#### login

Axon si basa sull'elenco provider di [Models.dev](https://models.dev), quindi puoi usare `axon auth login` per configurare le API key per qualunque provider tu voglia usare. Le credenziali vengono salvate in `~/.local/share/axon/auth.json`.

```bash
axon auth login
```

Quando Axon si avvia, carica i provider dal file delle credenziali e, se presenti, anche eventuali key definite nell'ambiente o in un file `.env` nel progetto.

---

#### list

Elenca tutti i provider autenticati come salvati nel file delle credenziali.

```bash
axon auth list
```

Oppure la versione corta.

```bash
axon auth ls
```

---

#### logout

Esegue il logout da un provider rimuovendolo dal file delle credenziali.

```bash
axon auth logout
```

---

### github

Gestisci l'agente GitHub per l'automazione dei repository.

```bash
axon github [command]
```

---

#### install

Installa l'agente GitHub nel tuo repository.

```bash
axon github install
```

Questo configura il workflow GitHub Actions necessario e ti guida nel processo di configurazione. [Scopri di più](/docs/github).

---

#### run

Esegui l'agente GitHub. Tipicamente usato in GitHub Actions.

```bash
axon github run
```

##### Flag

| Flag                                  | Descrizione                                  |
| ------------------------------------- | -------------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | Evento GitHub mock per cui eseguire l'agente |
| <nobr><code>{"--token"}</code></nobr> | GitHub personal access token                 |

---

### mcp

Gestisci i server Model Context Protocol.

```bash
axon mcp [command]
```

---

#### add

Aggiungi un server MCP alla tua configurazione.

```bash
axon mcp add
```

Questo comando ti guida nell'aggiunta di un server MCP locale o remoto.

---

#### list

Elenca tutti i server MCP configurati e il loro stato di connessione.

```bash
axon mcp list
```

Oppure la versione corta.

```bash
axon mcp ls
```

---

#### auth

Autentica con un server MCP con OAuth abilitato.

```bash
axon mcp auth [name]
```

Se non fornisci un nome server, ti verrà chiesto di selezionare tra i server OAuth-capable disponibili.

Puoi anche elencare i server OAuth-capable e il loro stato di autenticazione.

```bash
axon mcp auth list
```

Oppure la versione corta.

```bash
axon mcp auth ls
```

---

#### logout

Rimuovi le credenziali OAuth per un server MCP.

```bash
axon mcp logout [name]
```

---

#### debug

Esegui debug di problemi di connessione OAuth per un server MCP.

```bash
axon mcp debug <name>
```

---

### models

Elenca tutti i modelli disponibili dai provider configurati.

```bash
axon models [provider]
```

Questo comando mostra tutti i modelli disponibili tra i provider configurati nel formato `provider/model`.

È utile per capire l'esatto nome del modello da usare nella [config](/docs/config/).

Puoi anche passare opzionalmente un ID provider per filtrare i modelli a quel provider.

```bash
axon models anthropic
```

#### Flag

| Flag                                    | Descrizione                                        |
| --------------------------------------- | -------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Aggiorna la cache modelli da models.dev            |
| <nobr><code>{"--verbose"}</code></nobr> | Output più verboso (include metadati come i costi) |

Usa `--refresh` per aggiornare l'elenco modelli in cache. È utile quando nuovi modelli vengono aggiunti a un provider e vuoi vederli in Axon.

```bash
axon models --refresh
```

---

### run

Esegui axon in modalità non interattiva passando un prompt direttamente.

```bash
axon run [message..]
```

È utile per scripting, automazione o quando vuoi una risposta rapida senza avviare la TUI completa. Per esempio:

```bash "axon run"
axon run Explain the use of context in Go
```

Puoi anche collegarti a una istanza `axon serve` già in esecuzione per evitare i cold boot dei server MCP ad ogni esecuzione:

```bash
# Avvia un server headless in un terminale
axon serve

# In un altro terminale, esegui comandi che si collegano ad esso
axon run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flag

| Flag                                     | Breve | Descrizione                                                                                     |
| ---------------------------------------- | ----- | ----------------------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |       | Il comando da eseguire; usa message per gli argomenti                                           |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continua l'ultima sessione                                                                      |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID sessione da continuare                                                                       |
| <nobr><code>{"--fork"}</code></nobr>     |       | Duplica la sessione quando continui (usa con `--continue` o `--session`)                        |
| <nobr><code>{"--share"}</code></nobr>    |       | Condividi la sessione                                                                           |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Modello nel formato provider/model                                                              |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agente da usare                                                                                 |
| <nobr><code>{"--file"}</code></nobr>     | `-f`  | File da allegare al messaggio                                                                   |
| <nobr><code>{"--format"}</code></nobr>   |       | Formato: default (formattato) o json (eventi JSON grezzi)                                       |
| <nobr><code>{"--title"}</code></nobr>    |       | Titolo sessione (usa prompt troncato se non viene fornito un valore)                            |
| <nobr><code>{"--attach"}</code></nobr>   |       | Attach a un server axon in esecuzione (es. http://localhost:4096)                           |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Password per l'autenticazione di base (predefinita: `AXON_SERVER_PASSWORD`)                 |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Nome utente per l'autenticazione di base (predefinito: `AXON_SERVER_USERNAME` o `axon`) |
| <nobr><code>{"--dir"}</code></nobr>      |       | Directory di esecuzione, o percorso sul server remoto durante il collegamento                   |
| <nobr><code>{"--variant"}</code></nobr>  |       | Variante del modello (sforzo di ragionamento specifico del provider)                            |
| <nobr><code>{"--thinking"}</code></nobr> |       | Mostra blocchi di pensiero                                                                      |
| <nobr><code>{"--port"}</code></nobr>     |       | Porta per il server locale (di default una porta casuale)                                       |

---

### serve

Avvia un server Axon headless per accesso via API. Vedi le [server docs](/docs/server) per l'interfaccia HTTP completa.

```bash
axon serve
```

Avvia un server HTTP che espone accesso API alle funzionalità di axon senza la TUI. Imposta `AXON_SERVER_PASSWORD` per abilitare HTTP basic auth (username di default `axon`).

#### Flag

| Flag                                     | Descrizione                                    |
| ---------------------------------------- | ---------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Porta su cui mettersi in ascolto               |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname su cui mettersi in ascolto            |
| <nobr><code>{"--mdns"}</code></nobr>     | Abilita discovery mDNS                         |
| <nobr><code>{"--cors"}</code></nobr>     | Origin browser addizionali per consentire CORS |

---

### session

Gestisci le sessioni Axon.

```bash
axon session [command]
```

---

#### list

Elenca tutte le sessioni Axon.

```bash
axon session list
```

##### Flag

| Flag                                      | Breve | Descrizione                          |
| ----------------------------------------- | ----- | ------------------------------------ |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`  | Limita alle N sessioni più recenti   |
| <nobr><code>{"--format"}</code></nobr>    |       | Formato output: table o json (table) |

---

### stats

Mostra statistiche di utilizzo token e costo per le sessioni Axon.

```bash
axon stats
```

#### Flag

| Flag                                    | Descrizione                                                                           |
| --------------------------------------- | ------------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Mostra statistiche per gli ultimi N giorni (all time)                                 |
| <nobr><code>{"--tools"}</code></nobr>   | Numero di strumenti da mostrare (all)                                                 |
| <nobr><code>{"--models"}</code></nobr>  | Mostra breakdown di utilizzo modelli (nascosto di default). Passa un numero per top N |
| <nobr><code>{"--project"}</code></nobr> | Filtra per progetto (tutti i progetti; stringa vuota: progetto corrente)              |

---

### export

Esporta i dati di sessione come JSON.

```bash
axon export [sessionID]
```

Se non fornisci un ID sessione, ti verrà chiesto di selezionare tra le sessioni disponibili.

---

### import

Importa i dati di sessione da un file JSON o da un URL di condivisione Axon.

```bash
axon import <file>
```

Puoi importare da un file locale o da un URL di condivisione Axon.

```bash
axon import session.json
axon import https://opncd.ai/s/abc123
```

---

### web

Avvia un server Axon headless con interfaccia web.

```bash
axon web
```

Avvia un server HTTP e apre un browser per accedere a Axon tramite interfaccia web. Imposta `AXON_SERVER_PASSWORD` per abilitare HTTP basic auth (username di default `axon`).

#### Flag

| Flag                                     | Descrizione                                    |
| ---------------------------------------- | ---------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Porta su cui mettersi in ascolto               |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname su cui mettersi in ascolto            |
| <nobr><code>{"--mdns"}</code></nobr>     | Abilita discovery mDNS                         |
| <nobr><code>{"--cors"}</code></nobr>     | Origin browser addizionali per consentire CORS |

---

### acp

Avvia un server ACP (Agent Client Protocol).

```bash
axon acp
```

Questo comando avvia un server ACP che comunica via stdin/stdout usando nd-JSON.

#### Flag

| Flag                                     | Descrizione                         |
| ---------------------------------------- | ----------------------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Directory di lavoro                 |
| <nobr><code>{"--port"}</code></nobr>     | Porta su cui mettersi in ascolto    |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname su cui mettersi in ascolto |

---

### uninstall

Disinstalla Axon e rimuove tutti i file correlati.

```bash
axon uninstall
```

#### Flag

| Flag                                        | Breve | Descrizione                                  |
| ------------------------------------------- | ----- | -------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`  | Mantieni i file di configurazione            |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`  | Mantieni dati di sessione e snapshot         |
| <nobr><code>{"--dry-run"}</code></nobr>     |       | Mostra cosa verrebbe rimosso senza rimuovere |
| <nobr><code>{"--force"}</code></nobr>       | `-f`  | Salta le richieste di conferma               |

---

### upgrade

Aggiorna axon all'ultima versione o a una versione specifica.

```bash
axon upgrade [target]
```

Per aggiornare all'ultima versione:

```bash
axon upgrade
```

Per aggiornare a una versione specifica:

```bash
axon upgrade v0.1.48
```

#### Flag

| Flag                                   | Breve | Descrizione                                               |
| -------------------------------------- | ----- | --------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`  | Metodo di installazione usato: curl, npm, pnpm, bun, brew |

---

## Flag globali

La CLI di axon accetta i seguenti flag globali.

| Flag                                       | Breve | Descrizione                            |
| ------------------------------------------ | ----- | -------------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h`  | Mostra l'help                          |
| <nobr><code>{"--version"}</code></nobr>    | `-v`  | Stampa il numero di versione           |
| <nobr><code>{"--print-logs"}</code></nobr> |       | Stampa i log su stderr                 |
| <nobr><code>{"--log-level"}</code></nobr>  |       | Livello log (DEBUG, INFO, WARN, ERROR) |

---

## Variabili d'ambiente

Axon può essere configurato tramite variabili d'ambiente.

| Variabile                             | Tipo    | Descrizione                                                 |
| ------------------------------------- | ------- | ----------------------------------------------------------- |
| `AXON_AUTO_SHARE`                 | boolean | Condivide automaticamente le sessioni                       |
| `AXON_GIT_BASH_PATH`              | string  | Percorso all'eseguibile Git Bash su Windows                 |
| `AXON_CONFIG`                     | string  | Percorso al file di configurazione                          |
| `AXON_TUI_CONFIG`                 | string  | Percorso al file di configurazione TUI                      |
| `AXON_CONFIG_DIR`                 | string  | Percorso alla directory di configurazione                   |
| `AXON_CONFIG_CONTENT`             | string  | Contenuto JSON di config inline                             |
| `AXON_DISABLE_AUTOUPDATE`         | boolean | Disabilita i controlli automatici di aggiornamento          |
| `AXON_DISABLE_PRUNE`              | boolean | Disabilita la potatura dei dati vecchi                      |
| `AXON_DISABLE_TERMINAL_TITLE`     | boolean | Disabilita aggiornamenti automatici del titolo terminale    |
| `AXON_PERMISSION`                 | string  | Config permessi JSON inline                                 |
| `AXON_DISABLE_DEFAULT_PLUGINS`    | boolean | Disabilita i plugin di default                              |
| `AXON_DISABLE_LSP_DOWNLOAD`       | boolean | Disabilita download automatico dei server LSP               |
| `AXON_ENABLE_EXPERIMENTAL_MODELS` | boolean | Abilita modelli sperimentali                                |
| `AXON_DISABLE_AUTOCOMPACT`        | boolean | Disabilita compaction automatica del contesto               |
| `AXON_DISABLE_CLAUDE_CODE`        | boolean | Disabilita lettura da `.claude` (prompt + skill)            |
| `AXON_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disabilita lettura di `~/.claude/CLAUDE.md`                 |
| `AXON_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disabilita caricamento di `.claude/skills`                  |
| `AXON_DISABLE_MODELS_FETCH`       | boolean | Disabilita fetch dei modelli da fonti remote                |
| `AXON_FAKE_VCS`                   | string  | Provider VCS finto per scopi di test                        |
| `AXON_CLIENT`                     | string  | Identificatore client (default `cli`)                       |
| `AXON_ENABLE_EXA`                 | boolean | Abilita gli strumenti di web search Exa                     |
| `AXON_SERVER_PASSWORD`            | string  | Abilita basic auth per `serve`/`web`                        |
| `AXON_SERVER_USERNAME`            | string  | Sovrascrive lo username basic auth (default `axon`)     |
| `AXON_MODELS_URL`                 | string  | URL personalizzato per recuperare la configurazione modelli |

---

### Sperimentale

Queste variabili d'ambiente abilitano funzionalità sperimentali che potrebbero cambiare o essere rimosse.

| Variabile                                       | Tipo    | Descrizione                                             |
| ----------------------------------------------- | ------- | ------------------------------------------------------- |
| `AXON_EXPERIMENTAL`                         | boolean | Abilita funzionalità sperimentali sotto il flag globale |
| `AXON_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Abilita icon discovery                                  |
| `AXON_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disabilita copy on select nella TUI                     |
| `AXON_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Timeout di default per comandi bash in ms               |
| `AXON_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Massimo token di output per risposte LLM                |
| `AXON_EXPERIMENTAL_FILEWATCHER`             | boolean | Abilita file watcher per l'intera dir                   |
| `AXON_EXPERIMENTAL_OXFMT`                   | boolean | Abilita formatter oxfmt                                 |
| `AXON_EXPERIMENTAL_LSP_TOOL`                | boolean | Abilita strumento LSP sperimentale                      |
| `AXON_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disabilita file watcher                                 |
| `AXON_EXPERIMENTAL_EXA`                     | boolean | Abilita funzionalità Exa sperimentali                   |
| `AXON_EXPERIMENTAL_LSP_TY`                  | boolean | Abilita TY LSP per i file python                        |
| `AXON_EXPERIMENTAL_PLAN_MODE`               | boolean | Abilita plan mode                                       |
| `AXON_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Abilita task subagent in background                     |
| `AXON_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Abilita sistema eventi sperimentale                     |
| `AXON_EXPERIMENTAL_NATIVE_LLM`              | boolean | Abilita percorso nativo di richiesta LLM                |
| `AXON_EXPERIMENTAL_PARALLEL`                | boolean | Abilita esecuzione parallela della ricerca web          |
| `AXON_EXPERIMENTAL_SCOUT`                   | boolean | Abilita subagent Scout                                  |
| `AXON_EXPERIMENTAL_WORKSPACES`              | boolean | Abilita supporto workspace                              |
